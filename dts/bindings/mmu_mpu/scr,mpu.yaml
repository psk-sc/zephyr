description: Syntacore MPU (Memory Protection Unit)

compatible: "scr,mpu"

include: base.yaml

properties:
    reg:
      required: true

    num-mpu-regions:
      required: true
      type: int
      description: number of MPU regions supported by hardware
